package ${pkgPath};

import org.springframework.stereotype.Repository;

import com.lang.ftd.common.tools.mybatisUtils.pagination.PageMyBatis;
import com.lang.ftd.common.tools.mybatisUtils.pagination.PagingCriteria;
import com.lang.ftd.common.tools.dbtools.base.dao.impl.BaseDaoImpl;
import ${daoPath};
import ${modelPath};

@Repository("${clzNameLC}DaoImpl")
public class ${clzName}DaoImpl extends BaseDaoImpl<${clzName}> implements ${clzName}Dao {
	private static final String SELECT_BY_PAGE = "${modelPath}.selectByPage";
	
	@Override
	public PageMyBatis<${clzName}> selectByPage(PagingCriteria criteria) {
		return new PageMyBatis<${clzName}>( getSqlSession().selectList(SELECT_BY_PAGE, criteria));
	}
}
